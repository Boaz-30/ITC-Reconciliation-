import React from 'react';

export default function Charts({ data }) {
  // Simple placeholder chart (replace with actual chart library if needed)
  return (
    <div className="rounded-lg bg-white p-4 shadow">
      <h3 className="mb-2 text-lg font-medium">Transaction Overview</h3>
      <div className="h-32 bg-gray-100 flex items-center justify-center text-gray-500">
        [Chart placeholder – you can add recharts or chart.js here]
      </div>
    </div>
  );
}